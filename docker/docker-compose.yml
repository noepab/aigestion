version: '3.9'

services:
  # Frontend + Backend Application
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dev
    container_name: NEXUS V1-app-dev
    ports:
      - '5173:5173' # Vite dev server
      - '3000:3000' # Backend API
    volumes:
      - ..:/app
      - /app/node_modules
      - /app/server/node_modules
    environment:
      - NODE_ENV=development
      - VITE_API_URL=http://localhost:3000
      - DATABASE_URL=mongodb://mongodb:27017/NEXUS V1_dashboard
      - RABBITMQ_URL=amqp://rabbitmq:5672
      - REDIS_URL=redis://redis:6379
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - JWT_SECRET=${JWT_SECRET:-dev_secret_key_change_in_production}
      # Email Configuration for YouTube Transcriptions
      - EMAIL_HOST=${EMAIL_HOST:-smtp.gmail.com}
      - EMAIL_PORT=${EMAIL_PORT:-587}
      - EMAIL_USERNAME=${EMAIL_USERNAME}
      - EMAIL_PASSWORD=${EMAIL_PASSWORD}
      - EMAIL_FROM=${EMAIL_FROM:-noreply@www.NEXUS V1.net}
      - YOUTUBE_TRANSCRIPTION_EMAIL=${YOUTUBE_TRANSCRIPTION_EMAIL:-${EMAIL_USERNAME}}
      # OpenTelemetry Configuration
      - OTEL_SERVICE_NAME=NEXUS V1-backend
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger:4318
      - OTEL_NODE_RESOURCE_DETECTORS=env,host,os
    depends_on:
      mongodb:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - NEXUS V1-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:3000/health']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M

  # MongoDB Database
  mongodb:
    image: mongo:7-jammy
    container_name: NEXUS V1-mongodb
    ports:
      - '27017:27017'
    volumes:
      - mongodb-data:/data/db
      - mongodb-config:/data/configdb
      - ../.docker/mongo-init:/docker-entrypoint-initdb.d
    environment:
      - MONGO_INITDB_ROOT_USERNAME=${MONGO_ROOT_USER:-admin}
      - MONGO_INITDB_ROOT_PASSWORD=${MONGO_ROOT_PASSWORD:-admin123}
      - MONGO_INITDB_DATABASE=NEXUS V1_dashboard
    networks:
      - NEXUS V1-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'mongosh', '--eval', "db.adminCommand('ping')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '1.5'
          memory: 2G
        reservations:
          cpus: '0.25'
          memory: 512M

  # RabbitMQ Message Broker
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: NEXUS V1-rabbitmq
    ports:
      - '5672:5672' # AMQP port
      - '15672:15672' # Management UI
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq
      - ../.docker/rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf
    environment:
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_USER:-admin}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_PASS:-admin123}
      - RABBITMQ_DEFAULT_VHOST=/
    networks:
      - NEXUS V1-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'rabbitmq-diagnostics', 'ping']
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  # Redis Cache
  redis:
    image: redis:7-alpine
    container_name: NEXUS V1-redis
    ports:
      - '6379:6379'
    volumes:
      - redis-data:/data
      - ../.docker/redis/redis.conf:/usr/local/etc/redis/redis.conf
    command: redis-server /usr/local/etc/redis/redis.conf
    networks:
      - NEXUS V1-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.1'
          memory: 128M

  # Jaeger - Distributed Tracing (OpenTelemetry)
  jaeger:
    image: jaegertracing/all-in-one:1.53
    container_name: NEXUS V1-jaeger
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    ports:
      - '16686:16686' # UI
      - '4317:4317' # OTLP gRPC
      - '4318:4318' # OTLP HTTP
    networks:
      - NEXUS V1-network
    restart: unless-stopped
    healthcheck:
      test: ['CMD', 'wget', '--spider', 'http://localhost:14269/']
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.1'
          memory: 128M

  # Nginx Reverse Proxy (Optional for production-like setup)
  nginx:
    image: nginx:alpine
    container_name: NEXUS V1-nginx
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - ../.docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../.docker/nginx/conf.d:/etc/nginx/conf.d:ro
      - ../.docker/ssl:/etc/nginx/ssl:ro
      - ../dist:/usr/share/nginx/html:ro
    depends_on:
      - app
    networks:
      - NEXUS V1-network
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M
    profiles:
      - production

  # Python Evaluation Service
  evaluation:
    build:
      context: ..
      dockerfile: .docker/Dockerfile.evaluation
    container_name: NEXUS V1-evaluation
    volumes:
      - ../evaluation:/app/evaluation
      - evaluation-results:/app/evaluation/results
    environment:
      - PYTHONUNBUFFERED=1
      - WINDSURF_API_TOKEN=${WINDSURF_API_TOKEN}
      - WINDSURF_API_URL=${WINDSURF_API_URL}
    networks:
      - NEXUS V1-network
    restart: unless-stopped
    profiles:
      - evaluation

networks:
  NEXUS V1-network:
    driver: bridge
    name: NEXUS V1-network

volumes:
  mongodb-data:
    name: NEXUS V1-mongodb-data
  mongodb-config:
    name: NEXUS V1-mongodb-config
  rabbitmq-data:
    name: NEXUS V1-rabbitmq-data
  redis-data:
    name: NEXUS V1-redis-data
  evaluation-results:
    name: NEXUS V1-evaluation-results
