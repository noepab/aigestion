name: "Bundle Analysis"
on:
  pull_request:
    branches: [ main, master ]
    paths:
      - 'frontend/**/*'
      - 'pnpm-lock.yaml'

permissions:
  contents: read
  pull-requests: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - name: Set up Node.js
        uses: actions/setup-node@65d868f8d4d85d7d4abb7de0875cde3fcc8798f5 # v4.1.0
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci
      - name: Build and Analyze
        working-directory: ./frontend
        run: |
          npm run build
          # Placeholder for actual analysis action
          # Add custom analysis script here if needed
      # Placeholder for actual analysis action
