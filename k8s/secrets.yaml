# IMPORTANT: This is a template file. DO NOT commit with real secrets!
# Use kubectl create secret or external secrets management (Sealed Secrets, Vault, etc.)
#
# To create secrets from command line:
# kubectl create secret generic alejandro-secrets \
#   --from-literal=JWT_SECRET=your-jwt-secret \
#   --from-literal=GEMINI_API_KEY=your-api-key \
#   --from-literal=WINDSURF_API_TOKEN=your-token \
#   --from-literal=MONGO_ROOT_PASSWORD=your-mongo-password \
#   --from-literal=RABBITMQ_DEFAULT_PASS=your-rabbitmq-password \
#   --from-literal=REDIS_PASSWORD=your-redis-password \
#   -n alejandro

apiVersion: v1
kind: Secret
metadata:
  name: NEXUS V1-secrets
  namespace: NEXUS V1
type: Opaque
stringData:
  # REPLACE THESE WITH REAL VALUES OR USE EXTERNAL SECRETS MANAGEMENT
  JWT_SECRET: 'REPLACE_WITH_STRONG_SECRET'
  GEMINI_API_KEY: 'REPLACE_WITH_YOUR_API_KEY'
  WINDSURF_API_TOKEN: 'REPLACE_WITH_YOUR_TOKEN'
  MONGO_ROOT_PASSWORD: 'REPLACE_WITH_STRONG_PASSWORD'
  MONGODB_URI: 'mongodb://admin:REPLACE_WITH_STRONG_PASSWORD@mongodb-service:27017/alejandro_db?authSource=admin'
  RABBITMQ_DEFAULT_USER: 'admin'
  RABBITMQ_DEFAULT_PASS: 'REPLACE_WITH_STRONG_PASSWORD'
  RABBITMQ_URI: 'amqp://admin:REPLACE_WITH_STRONG_PASSWORD@rabbitmq-service:5672'
  REDIS_PASSWORD: 'REPLACE_WITH_STRONG_PASSWORD'


